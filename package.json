{
  "name": "prompt-ping",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build:content": "esbuild src/content/bootstrap.js --bundle --format=iife --platform=browser --outfile=src/content/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
